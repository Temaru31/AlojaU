# Frontend env — nunca subir .env real (ver .gitignore)
# Local: cp .env.example .env
# Producción (Vercel): poner VITE_API_URL=https://alojau-api.onrender.com en dashboard Vercel → Settings → Environment Variables
VITE_API_URL=http://localhost:8000

# v13 Google OAuth (Supabase Auth). Vacío = botón muestra guía de configuración.
# En Vercel prod: VITE_SUPABASE_URL=https://xxx.supabase.co y VITE_SUPABASE_ANON_KEY=...
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
