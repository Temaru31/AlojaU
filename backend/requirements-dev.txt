# Dependencias de desarrollo/test del backend (CI + local).
# Producción (Dockerfile/Render) usa SOLO requirements.txt para una imagen ligera.
# CI instala ambas: pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

# Runner de tests (backend/tests/ usa pytest + TestClient).
pytest>=8,<9
# Pedidos por la rúbrica CI (los tests actuales son sync; quedan fijados para futuro uso async).
pytest-asyncio>=0.23,<1
anyio>=4.15.1,<5
# Cobertura con umbral mínimo 70 % (--cov-fail-under=70 en CI).
pytest-cov>=5,<7
