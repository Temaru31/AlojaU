# Raíz - ejemplo solo informativo
# Backend usa backend/.env
# Frontend usa frontend/.env (VITE_API_URL)
DATABASE_URL=postgresql+asyncpg://alojau:alojau123@localhost:5432/alojau
VITE_API_URL=http://localhost:8000
# PROD fail-closed RLS/SEC (ver backend/.env.example):
# ENV=prod
# SECRET_KEY=<openssl rand -hex 32, >=32 chars, nunca default>
# USE_MOCK_FALLBACK=False
